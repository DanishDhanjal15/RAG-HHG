# Chunking ablation

Generated by `bench/run_retrieval_eval.py`. Gold labels are the dataset's own
`is_selected` flags, so nothing here is hand-annotated.

- corpus: **17,708 chunks** over 15,000 queries
- eval set: **20 queries** held out by seed 20260814
- metrics are **document-level**: chunks are collapsed to their source passage
  (best rank wins) before scoring, so a view is not rewarded for shredding
  documents into more pieces
- each single-view arm searches with a FAISS ID selector restricted to that
  view, so every arm gets its own full top-k

### Reading the single-view rows

Views do not all cover the whole corpus, and recall must be read with that in
mind. `fixed_overlap` only emits chunks for passages above its token floor
(~35% of them), and `sentence_window` and `semantic` skip passages too short to
split. A low R@20 for those arms is partly a **coverage ceiling** -- they cannot
retrieve a document they never chunked -- not purely a ranking failure. Only
`atomic` covers every passage by construction.

That is a fair comparison of *what each view contributes to the index*, which is
the decision being made here, but it is not a claim that one chunking algorithm
ranks better than another in isolation.

## Verdict

- best single view: **atomic** (nDCG@10 0.506)
- best fused config: **fused-hybrid+rerank** (nDCG@10 0.472)

**Multi-view fusion does NOT beat the best single view here.** Uniform RRF assumes every run is comparably good; when one view is much weaker, averaging it in drags the strong one down. The weighting sweep below is the response — and if no weighting recovers it, the honest conclusion is to ship fewer views.

| arm | R@1 | R@5 | R@20 | MRR@10 | nDCG@10 | p50 ms | note |
|---|---|---|---|---|---|---|---|
| `atomic` | 0.450 | 0.850 | 0.950 | 0.602 | 0.506 | 15.8 | single view, dense only |
| `sentence_window` | 0.500 | 0.750 | 0.850 | 0.598 | 0.484 | 14.8 | single view, dense only |
| `fixed_overlap` | 0.250 | 0.350 | 0.350 | 0.285 | 0.245 | 14.4 | single view, dense only |
| `semantic` | 0.250 | 0.350 | 0.550 | 0.321 | 0.287 | 13.1 | single view, dense only |
| `fused-dense` | 0.250 | 0.800 | 0.950 | 0.457 | 0.397 | 15.4 | all views, RRF, dense only |
| `fused-hybrid` | 0.250 | 0.750 | 0.950 | 0.465 | 0.400 | 19.4 | all views, RRF, dense + BM25 |
| `fused-hybrid+rerank` | 0.500 | 0.750 | 0.950 | 0.607 | 0.472 | 192.5 | production config |
| `w:uniform` | 0.250 | 0.750 | 0.950 | 0.465 | 0.400 | 17.1 | every view weighted equally |
| `w:proportional` | 0.200 | 0.750 | 0.950 | 0.450 | 0.403 | 19.5 | weight ∝ that view's own nDCG@10 |
| `w:proportional-squared` | 0.200 | 0.750 | 0.950 | 0.451 | 0.413 | 16.6 | as above, squared -- sharper preference for strong views |
| `w:drop-fixed_overlap` | 0.250 | 0.750 | 0.950 | 0.471 | 0.403 | 15.9 | only atomic, semantic, sentence_window |
| `w:drop-semantic+fixed_overlap` | 0.250 | 0.750 | 0.950 | 0.475 | 0.415 | 16.3 | only atomic, sentence_window |
| `w:drop-sentence_window+semantic+fixed_overlap` | 0.200 | 0.750 | 0.950 | 0.452 | 0.433 | 17.3 | only atomic |
